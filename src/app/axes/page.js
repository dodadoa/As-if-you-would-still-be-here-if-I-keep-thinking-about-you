import AxesCanvas from "./AxesCanvas";

export const metadata = {
  title: "Rotating Axes",
  description: "Interactive p5-style axes that follow your mouse and spin on click.",
};

export default function AxesPage() {
  return <AxesCanvas />;
}
